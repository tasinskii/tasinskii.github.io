{
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "homepage": "https://tasinskii.github.io/reprally-app",
    "scripts": {
      "predeploy": "npm run build",
      "deploy": "gh-pages -d build"
    }
  }
}
